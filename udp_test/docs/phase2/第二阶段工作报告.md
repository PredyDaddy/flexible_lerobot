# 第二阶段工作报告

本文档记录第二阶段 `JZRobotUDP` command dry-run 链路的开发结果，方便后续在 x86 与 Orin 之间继续联调。

当前阶段的核心结论：

```text
JZRobotUDP.send_action() 已经实现，但只允许 dry_run。
x86 可以验证 LeRobot action path 和 UDP command packet。
Orin 只接收、解码、校验、打印 command 日志。
Orin 不 publish ROS command topic，不 publish cmd_vel，不控制手臂、夹爪或底盘。
```

注意：本文档记录的是 Phase 2 当前完成状态。Phase 2 的本机 dry-run 和安全审查已经通过；x86/Orin 实机 command dry-run、`lerobot-record` dry-run 短 episode、Phase 1 `OBS_COUNT=600` 回归仍需要在实机链路上补充验收证据。

## 阶段目标

第一阶段已经完成 observation-only 链路：

```text
Orin / ARM:
  ROS2 state topic -> UDP state packet

x86:
  UDP state packet + RTSP camera -> JZRobotUDP.get_observation()
```

第二阶段的目标是在不让机器人运动的前提下，把 LeRobot action path 打通：

```text
x86 JZRobotUDP.send_action()
  -> action key / value 校验
  -> command packet v1 编码
  -> local dry-run log 或 UDP dry-run 发送
  -> Orin command receiver 接收
  -> sender IP / packet schema / seq 校验
  -> Orin 打印 DRY_RUN command 日志
  -> 到此为止
```

第二阶段明确不做：

```text
不把 command 转成 ROS command topic
不 create_publisher
不 publish(...)
不 publish cmd_vel
不控制机械臂
不控制夹爪
不控制底盘
不做 policy inference 实机控制
不把人工急停作为唯一安全机制
```

## 当前网络约定

```text
Orin 网线口 IP: 192.168.1.81
x86  网线口 IP: 192.168.1.106
UDP state port:   39010
UDP command port: 39020
RTSP port:        8554
```

当前 command dry-run 默认方向：

```text
x86 192.168.1.106 -> Orin 192.168.1.81:39020
```

Orin command receiver 默认只接受：

```text
allowed_sender_ip=192.168.1.106
```

## 改动总览

### 1. JZRobotUDP 配置增加 command dry-run 字段

修改文件：

```text
src/lerobot/robots/jz_robot_udp/config_jz_robot_udp.py
src/lerobot/configs/robot/jz_robot_udp_three_rtsp.yaml
```

新增配置语义：

```text
command_target_ip: 192.168.1.81
command_target_port: 39020
send_action_transport: local | udp
send_action_execution: dry_run
command_robot: robot1
command_timeout_s: 0.2
```

当前安全约束：

```text
send_action_execution 只能是 dry_run。
send_action_transport 只能是 local 或 udp。
active / armed / execute / publish 等真实执行语义会被配置校验拒绝。
```

这一步的意义是把 `send_action()` 的接口能力补齐，但把执行模式固定在 Phase 2 的 dry-run 范围内。

### 2. 新增 command packet v1 协议

修改文件：

```text
src/lerobot/robots/jz_robot_udp/protocol.py
```

新增内容：

```text
COMMAND_MESSAGE_TYPE = "command"
COMMAND_MODE_DRY_RUN = "dry_run"

make_jz_robot_udp_command_packet(...)
encode_jz_robot_udp_command_packet(...)
decode_jz_robot_udp_command_packet(...)
validate_jz_robot_udp_command_packet(...)
```

command packet 使用 JSON UTF-8 编码，字段结构：

```json
{
  "version": 1,
  "type": "command",
  "robot": "robot1",
  "seq": 1,
  "stamp_ns": 123456789,
  "mode": "dry_run",
  "actions": {
    "left": {
      "left_joint1": 0.0
    },
    "right": {
      "right_joint1": 0.0
    },
    "grippers": {
      "left": {
        "width": 0.0,
        "force": 0.0
      },
      "right": {
        "width": 0.0,
        "force": 0.0
      }
    }
  }
}
```

协议校验覆盖：

```text
version/type
robot
seq
stamp_ns
mode=dry_run
actions.left
actions.right
actions.grippers.left/right
数值类型
bool 拒绝
NaN / inf 拒绝
```

当前 receiver 会检测 seq 单调递增；`stamp_ns` 当前校验存在和类型，不做基于当前时间的 stale-age 拒绝。由于 Phase 2 不执行动作，这不是当前安全阻塞；进入 Phase 3 前应再补时间窗安全门。

### 3. 新增 UDP command sender

修改文件：

```text
src/lerobot/robots/jz_robot_udp/udp_client.py
```

新增类：

```text
UDPCommandSender
```

作用：

```text
只负责把已经编码好的 dry-run command bytes 通过 UDP sendto() 发到目标 IP/port。
不接收 state。
不 import ROS。
不 publish ROS message。
不包含任何机器人执行语义。
```

状态接收和 command 发送保持分离：

```text
UDPStateReceiver:
  bind + recvfrom state packet

UDPCommandSender:
  sendto command packet
```

### 4. JZRobotUDP action_features 和 send_action

修改文件：

```text
src/lerobot/robots/jz_robot_udp/jz_robot_udp.py
```

`action_features` 现在包含 18 个字段：

```text
left_left_joint1.pos
left_left_joint2.pos
left_left_joint3.pos
left_left_joint4.pos
left_left_joint5.pos
left_left_joint6.pos
left_left_joint7.pos

right_right_joint1.pos
right_right_joint2.pos
right_right_joint3.pos
right_right_joint4.pos
right_right_joint5.pos
right_right_joint6.pos
right_right_joint7.pos

left_gripper.width
left_gripper.force
right_gripper.width
right_gripper.force
```

`send_action()` 当前行为：

```text
1. 校验 action keys 必须精确匹配 action_features
2. 拒绝 missing key
3. 拒绝 extra key
4. 拒绝 bool / 非数值
5. 转成 float action copy
6. seq 自增
7. 固定 mode=dry_run
8. 生成 command packet
9. local 模式只打印 DRY_RUN 日志
10. udp 模式只发送 dry-run UDP packet
11. 返回 float action copy
```

`send_action()` 不做：

```text
不调用 ROS
不调用机器人底层 API
不 publish command topic
不 publish cmd_vel
不控制真实机器人
```

### 5. 新增 Orin command dry-run receiver

新增文件：

```text
udp_test/test_scripts/arm_side/orin_udp_command_receiver.py
```

用途：

```text
在 Orin 上监听 UDP command port 39020。
只接收 x86 发来的 dry-run command packet。
只做解码、校验、统计、打印日志。
```

启动时必须打印：

```text
PHASE2 COMMAND DRY-RUN ONLY
NOT publishing ROS command topics
robot will not move
```

receiver 行为：

```text
bind --bind-ip 192.168.1.81 --port 39020
校验 --allowed-sender-ip 192.168.1.106
decode command packet
拒绝 invalid packet
拒绝 unexpected sender
检测 seq 非单调
打印 DRY_RUN received 日志
SIGINT / SIGTERM 后干净退出
```

receiver 禁止能力：

```text
不 import rclpy
不 create_publisher
不 publish(...)
不 publish cmd_vel
不调用任何机器人控制 API
```

### 6. 新增 x86 send_action check 脚本

新增文件：

```text
udp_test/test_scripts/x86_side/x86_jz_robot_udp_send_action_check.py
```

用途：

```text
在 x86 上生成固定 0 action。
调用 JZRobotUDP.send_action()。
验证 local dry-run 或 UDP dry-run 发送链路。
```

默认是 `--command-only`：

```text
不连接 RTSP 相机
不等待 UDP state
不启动机器人控制链路
只测试 action_features / send_action / command packet
```

启动时打印：

```text
PHASE2 COMMAND DRY-RUN ONLY
This script does not enable robot execution.
```

### 7. 新增 JZRobotUDP constant teleop

新增文件：

```text
src/lerobot/teleoperators/jz_robot_udp_constant/__init__.py
src/lerobot/teleoperators/jz_robot_udp_constant/config_jz_robot_udp_constant.py
src/lerobot/teleoperators/jz_robot_udp_constant/jz_robot_udp_constant.py
```

修改文件：

```text
src/lerobot/teleoperators/utils.py
src/lerobot/scripts/lerobot_record.py
src/lerobot/teleoperators/teleoperator.py
```

用途：

```text
给 Phase 2 lerobot-record dry-run 提供安全 action source。
```

行为：

```text
teleop.type=jz_robot_udp_constant
只产生固定 action，默认全部为 0.0
action keys 与 JZRobotUDP.action_features 精确一致
不 import ROS
不打开串口
不连接手柄
不连接真实 teleop 硬件
不 publish command topic
```

默认输出 18 个 action 字段：

```text
left_left_joint1.pos ... left_left_joint7.pos
right_right_joint1.pos ... right_right_joint7.pos
left_gripper.width
left_gripper.force
right_gripper.width
right_gripper.force
```

这一步是为了让标准 `lerobot-record` 能在 Phase 2 使用安全固定动作跑完整 record 流程。它只验证 record/action/dataset 链路，不生成最终可训练示教数据。

`teleoperator.py` 同时做了一个导入稳定性修正：运行时不再为了类型标注 import `lerobot.processor`，避免 `teleoperators` 与 `processor.hil_processor` 的循环导入影响 CLI / factory smoke check。

### 8. 新增 Orin command receiver 启停脚本

新增 / 修改文件：

```text
udp_test/server_bash/orin_arm/start_command_receiver.sh
udp_test/server_bash/orin_arm/stop_command_receiver.sh
udp_test/server_bash/orin_arm/status.sh
udp_test/server_bash/README.md
```

Orin 启动脚本默认：

```text
ORIN_IP=192.168.1.81
X86_IP=192.168.1.106
COMMAND_PORT=39020
PYTHON_CMD="conda run --no-capture-output -n lerobot python"
AUTO_TAIL=1
```

说明：

```text
当前 ARM / Orin 侧可用 conda 环境是 lerobot。
x86 侧文档命令仍使用用户当前的 lerobot_flex 环境。
```

`status.sh` 现在会同时显示：

```text
orin_ros_state_udp_bridge.py
orin_udp_command_receiver.py
```

## 测试与验证

以下验证在 ARM 当前工作树完成，路径：

```text
/home/data/test/workspace/flexible_lerobot
```

### 1. 单元测试

命令：

```bash
PYTHONPATH=src PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 conda run --no-capture-output -n lerobot python -m pytest -q tests/robots/test_jz_robot_udp.py
```

结果：

```text
Testing with DEVICE='cpu'
..................................                                       [100%]
34 passed in 0.31s
```

覆盖内容：

```text
config dry-run 默认值
非 dry-run execution 拒绝
非法 transport 拒绝
command packet encode/decode/validate
bool / NaN / inf 拒绝
action_features 非空
local dry-run send_action
UDP dry-run send_action
Orin receiver AST 安全检查
```

### 2. Python 编译检查

命令：

```bash
PYTHONPATH=src conda run --no-capture-output -n lerobot python -m py_compile \
  src/lerobot/robots/jz_robot_udp/config_jz_robot_udp.py \
  src/lerobot/robots/jz_robot_udp/protocol.py \
  src/lerobot/robots/jz_robot_udp/udp_client.py \
  src/lerobot/robots/jz_robot_udp/jz_robot_udp.py \
  udp_test/test_scripts/arm_side/orin_udp_command_receiver.py \
  udp_test/test_scripts/x86_side/x86_jz_robot_udp_send_action_check.py \
  tests/robots/test_jz_robot_udp.py
```

结果：

```text
PASS，退出码 0
```

### 3. Bash 语法检查

命令：

```bash
bash -n \
  udp_test/server_bash/orin_arm/start_command_receiver.sh \
  udp_test/server_bash/orin_arm/stop_command_receiver.sh \
  udp_test/server_bash/orin_arm/status.sh
```

结果：

```text
PASS，退出码 0
```

### 4. 本机 127.0.0.1 UDP dry-run

receiver 命令：

```bash
PYTHONPATH=src conda run --no-capture-output -n lerobot python \
  udp_test/test_scripts/arm_side/orin_udp_command_receiver.py \
  --bind-ip 127.0.0.1 \
  --port 39020 \
  --allowed-sender-ip 127.0.0.1 \
  --count 5 \
  --print-every 1
```

sender 命令：

```bash
PYTHONPATH=src conda run --no-capture-output -n lerobot python \
  udp_test/test_scripts/x86_side/x86_jz_robot_udp_send_action_check.py \
  --robot-config src/lerobot/configs/robot/jz_robot_udp_three_rtsp.yaml \
  --transport udp \
  --execution dry_run \
  --command-only \
  --command-target-ip 127.0.0.1 \
  --command-target-port 39020 \
  --count 5 \
  --hz 20 \
  --print-every 1
```

sender 结果：

```text
PHASE2 COMMAND DRY-RUN ONLY
This script does not enable robot execution.
[x86 send_action check] config=/home/data/test/workspace/flexible_lerobot/src/lerobot/configs/robot/jz_robot_udp_three_rtsp.yaml transport=udp execution=dry_run command_only=True
[x86 send_action check] DRY_RUN sent idx=1 action_keys=18 returned_keys=18
[x86 send_action check] DRY_RUN sent idx=2 action_keys=18 returned_keys=18
[x86 send_action check] DRY_RUN sent idx=3 action_keys=18 returned_keys=18
[x86 send_action check] DRY_RUN sent idx=4 action_keys=18 returned_keys=18
[x86 send_action check] DRY_RUN sent idx=5 action_keys=18 returned_keys=18
SUMMARY: PASS commands=5
```

receiver 结果：

```text
PHASE2 COMMAND DRY-RUN ONLY
NOT publishing ROS command topics
robot will not move
[orin command receiver] bind=127.0.0.1:39020 allowed_sender_ip=127.0.0.1 count=5 buffer_size=65535
[orin command receiver] DRY_RUN received idx=1 seq=1 bytes=473 left_actions=7 right_actions=7 gripper_actions=4
[orin command receiver] DRY_RUN received idx=2 seq=2 bytes=473 left_actions=7 right_actions=7 gripper_actions=4
[orin command receiver] DRY_RUN received idx=3 seq=3 bytes=473 left_actions=7 right_actions=7 gripper_actions=4
[orin command receiver] DRY_RUN received idx=4 seq=4 bytes=473 left_actions=7 right_actions=7 gripper_actions=4
[orin command receiver] DRY_RUN received idx=5 seq=5 bytes=473 left_actions=7 right_actions=7 gripper_actions=4
SUMMARY: received=5 invalid=0 unexpected_sender=0 elapsed_s=10.565
```

结论：

```text
本机 dry-run command 软件链路通过。
command packet 能发送、接收、解码、校验。
receiver 没有 invalid packet。
receiver 没有 unexpected sender。
```

### 5. JZRobotUDP constant teleop 验证

命令：

```bash
PYTHONPATH=src PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 conda run --no-capture-output -n lerobot python -m pytest -q tests/robots/test_jz_robot_udp.py
```

结果：

```text
Testing with DEVICE='cpu'
....................................                                     [100%]
36 passed in 0.33s
```

新增覆盖内容：

```text
jz_robot_udp_constant teleop action_features 与 JZRobotUDP.action_features 一致
get_action() 默认返回 18 个 0.0 action
teleop factory 能通过 make_teleoperator_from_config 创建
TeleoperatorConfig 注册名为 jz_robot_udp_constant
```

CLI / import smoke check：

```bash
PYTHONPATH=src conda run --no-capture-output -n lerobot python -m lerobot.scripts.lerobot_record --help
```

结果：

```text
PASS，退出码 0
```

## 独立验收审查

第二阶段实现后，启动了新的 xhigh 子代理做只读验收审查。

审查范围包括：

```text
udp_test/docs/phase2/技术文档.md
udp_test/docs/phase2/plan.md
src/lerobot/robots/jz_robot_udp/config_jz_robot_udp.py
src/lerobot/robots/jz_robot_udp/protocol.py
src/lerobot/robots/jz_robot_udp/udp_client.py
src/lerobot/robots/jz_robot_udp/jz_robot_udp.py
src/lerobot/configs/robot/jz_robot_udp_three_rtsp.yaml
udp_test/test_scripts/arm_side/orin_udp_command_receiver.py
udp_test/test_scripts/x86_side/x86_jz_robot_udp_send_action_check.py
udp_test/server_bash/orin_arm/start_command_receiver.sh
udp_test/server_bash/orin_arm/stop_command_receiver.sh
udp_test/server_bash/orin_arm/status.sh
udp_test/server_bash/README.md
tests/robots/test_jz_robot_udp.py
```

子代理结论：

```text
未发现阻塞问题。
未发现真实控制路径。
未发现 Phase 2 安全边界破坏。
可以通知用户开始 x86/Orin command dry-run 联调。
不能宣称 Phase 2 最终实机验收已经完成。
```

子代理指出的非阻塞注意项：

```text
1. 文档里的 conda 环境名需要区分 ARM=lerobot、x86=lerobot_flex。
2. plan.md 是执行计划，不是最终验收记录，实际汇报应以验证输出为准。
3. receiver 当前只校验 stamp_ns 存在和类型，不做 stale-age 拒绝；Phase 3 前应补时间窗安全门。
```

## 当前安全边界

当前软件安全边界由多层约束组成：

```text
1. 配置层：
   send_action_execution 必须等于 dry_run。

2. 协议层：
   command packet mode 必须等于 dry_run。

3. x86 Robot 层：
   send_action() 固定使用 COMMAND_MODE_DRY_RUN 组包。

4. x86 UDP 层：
   UDPCommandSender 只 sendto bytes，不包含 ROS 或机器人 API。

5. Orin receiver 层：
   只 socket recvfrom + decode + validate + print。

6. 单测层：
   AST 检查禁止 rclpy、create_publisher、publish、cmd_vel 控制路径。
```

因此当前 Phase 2 可以验证 command 链路，但不能触发真实机器人动作。

## 当前可以开始的 x86/Orin 联调

现在可以做的联调是：

```text
x86 -> Orin command dry-run
```

不能做的联调是：

```text
真实机械臂动作
真实夹爪动作
真实底盘动作
policy inference 实机控制
Orin command -> ROS command publisher
```

### Orin / ARM 侧启动 receiver

```bash
cd /home/data/test/workspace/flexible_lerobot
bash udp_test/server_bash/orin_arm/start_command_receiver.sh
```

预期：

```text
PHASE2 COMMAND DRY-RUN ONLY
NOT publishing ROS command topics
robot will not move
```

### x86 侧发送 dry-run command

```bash
cd ~/cqy/aaa/flexible_lerobot

PYTHONPATH=src conda run --no-capture-output -n lerobot_flex python \
  udp_test/test_scripts/x86_side/x86_jz_robot_udp_send_action_check.py \
  --robot-config src/lerobot/configs/robot/jz_robot_udp_three_rtsp.yaml \
  --transport udp \
  --execution dry_run \
  --command-only \
  --command-target-ip 192.168.1.81 \
  --command-target-port 39020 \
  --count 20 \
  --hz 5
```

x86 侧预期：

```text
PHASE2 COMMAND DRY-RUN ONLY
This script does not enable robot execution.
SUMMARY: PASS commands=20
```

Orin 侧预期：

```text
DRY_RUN received idx=1 seq=1 ...
DRY_RUN received idx=20 seq=20 ...
SUMMARY: received=20 invalid=0 unexpected_sender=0
```

停止 receiver：

```bash
bash udp_test/server_bash/orin_arm/stop_command_receiver.sh
bash udp_test/server_bash/orin_arm/status.sh
```

## x86/Orin 实机验收结果

第二阶段后续在 x86 和 Orin 实机链路上完成了三层 dry-run 验收。

### 1. x86 -> Orin command dry-run

x86 命令：

```bash
PYTHONPATH=src conda run --no-capture-output -n lerobot_flex python \
  udp_test/test_scripts/x86_side/x86_jz_robot_udp_send_action_check.py \
  --robot-config src/lerobot/configs/robot/jz_robot_udp_three_rtsp.yaml \
  --transport udp \
  --execution dry_run \
  --command-only \
  --command-target-ip 192.168.1.81 \
  --command-target-port 39020 \
  --count 20 \
  --hz 5 \
  --print-every 1
```

x86 结果：

```text
PHASE2 COMMAND DRY-RUN ONLY
This script does not enable robot execution.
[x86 send_action check] DRY_RUN sent idx=1 action_keys=18 returned_keys=18
...
[x86 send_action check] DRY_RUN sent idx=20 action_keys=18 returned_keys=18
SUMMARY: PASS commands=20
```

Orin 结果：

```text
PHASE2 COMMAND DRY-RUN ONLY
NOT publishing ROS command topics
robot will not move
[orin command receiver] bind=192.168.1.81:39020 allowed_sender_ip=192.168.1.106 count=0 buffer_size=65535
[orin command receiver] DRY_RUN received idx=1 seq=1 bytes=473 left_actions=7 right_actions=7 gripper_actions=4
...
[orin command receiver] DRY_RUN received idx=20 seq=20 bytes=474 left_actions=7 right_actions=7 gripper_actions=4
```

结论：

```text
x86 send_action UDP dry-run 发包通过。
Orin command receiver 实机 bind 192.168.1.81:39020 通过。
Orin 收到 seq=1..20，idx=1..20。
action counts 正确：left=7, right=7, grippers=4。
未出现 invalid packet / unexpected sender。
机器人未执行动作。
```

### 2. Phase 1 observation 回归

x86 命令：

```bash
OBS_COUNT=600 bash udp_test/server_bash/x86/start.sh
```

Orin 命令：

```bash
bash udp_test/server_bash/orin_arm/start.sh
```

x86 结果：

```text
[x86 jz_robot_udp observation check] obs=600 keys=21 numeric=18 image_shapes={'camera_head': (720, 1280, 3), 'camera_left': (480, 640, 3), 'camera_right': (480, 640, 3)}
SUMMARY: PASS observations=600
```

Orin 结果：

```text
[orin ros state udp bridge] READONLY subscribe-only bridge. It does not publish command topics and does not call send_action.
[orin ros state udp bridge] local=192.168.1.81:<port> target=192.168.1.106:39010 hz=20.0
[orin ros state udp bridge] sent seq 持续增长
```

过程中 x86 侧出现过 H264 解码告警，例如：

```text
[h264 @ ...] corrupted macroblock ...
[h264 @ ...] error while decoding MB ...
```

该告警来自 FFmpeg/H264 对局部视频块的解码日志。由于 `get_observation()` 连续拿到 600 次完整 observation，且三路图像 shape 一直正确，因此本次 observation 回归仍判定通过。该告警不等价于 observation failure；真正阻塞条件是 TimeoutError、stale_count 持续增长、traceback、图像缺失或 shape 错误。

结论：

```text
Phase 2 改动没有破坏 Phase 1 observation-only 链路。
UDP state、三路 RTSP image、get_observation() 回归通过。
机器人未执行动作。
```

### 3. lerobot-record dry-run

Orin 先启动 command receiver：

```bash
COMMAND_COUNT=100 AUTO_TAIL=1 bash udp_test/server_bash/orin_arm/start_command_receiver.sh
```

Orin 再启动 state bridge：

```bash
bash udp_test/server_bash/orin_arm/start.sh
```

x86 启动 record dry-run：

```bash
bash x86_test.sh record_dry_run
```

x86 record 配置关键项：

```text
robot.type=jz_robot_udp
robot.send_action_transport=udp
robot.send_action_execution=dry_run
robot.command_target_ip=192.168.1.81
robot.command_target_port=39020
teleop.type=jz_robot_udp_constant
dataset.repo_id=local/jz_robot_udp_phase2_dry_run
dataset.root=tests/outputs/jz_robot_udp_phase2_dry_run
dataset.num_episodes=1
dataset.episode_time_s=10
dataset.fps=5
dataset.push_to_hub=false
```

x86 结果：

```text
INFO ... Recording episode 0
INFO ... JZRobotUDP DRY_RUN UDP command seq=1 mode=dry_run robot=robot1 action_key_count=18 target=192.168.1.81:39020 bytes=473
...
INFO ... JZRobotUDP DRY_RUN UDP command seq=50 mode=dry_run robot=robot1 action_key_count=18 target=192.168.1.81:39020 bytes=474
Map: 100%|...| 50/50
INFO ... Stop recording
INFO ... Exiting
```

Orin command receiver 结果：

```text
[orin command receiver] DRY_RUN received idx=31 seq=31 bytes=474 left_actions=7 right_actions=7 gripper_actions=4
...
[orin command receiver] DRY_RUN received idx=50 seq=50 bytes=474 left_actions=7 right_actions=7 gripper_actions=4
```

x86 dataset 落盘结果：

```text
tests/outputs/jz_robot_udp_phase2_dry_run/meta/stats.json
tests/outputs/jz_robot_udp_phase2_dry_run/meta/tasks.parquet
tests/outputs/jz_robot_udp_phase2_dry_run/meta/info.json
tests/outputs/jz_robot_udp_phase2_dry_run/data/chunk-000/file-000.parquet
```

本次 x86 环境出现 `pynput` headless warning：

```text
Error trying to import pynput. Switching to headless mode.
Headless environment detected.
```

这表示当前 x86 没有可用 X display，不能显示窗口或键盘控制流程。由于本次 dry-run 使用 `display_data=false`、`play_sounds=false` 和 `jz_robot_udp_constant` 固定动作源，该 warning 不影响 record 主流程。

结论：

```text
标准 lerobot-record 主流程可以完成 1 个 10 秒 dry-run episode。
x86 record 期间没有 send_action NotImplementedError。
x86 send_action 产生 seq=1..50 dry-run command。
Orin command receiver 收到 idx=1..50 / seq=1..50。
dataset 本地保存，未 push hub。
机器人未执行动作。
```

### 4. 收尾状态

Orin 收尾命令：

```bash
bash udp_test/server_bash/orin_arm/stop_command_receiver.sh
bash udp_test/server_bash/orin_arm/stop.sh
bash udp_test/server_bash/orin_arm/status.sh
```

Orin 收尾结果：

```text
[orin_arm/stop_command_receiver] stopped
[orin_arm/stop] stopped
[orin_arm/status] matching ROS state UDP bridge processes:
[orin_arm/status] matching UDP command dry-run receiver processes:
```

结论：

```text
ROS state UDP bridge 无残留。
UDP command dry-run receiver 无残留。
```

## 当前阶段结论

当前第二阶段已经完成：

```text
1. JZRobotUDP action_features 已实现
2. JZRobotUDP send_action 已实现
3. send_action 只允许 dry_run
4. command packet v1 已实现
5. local dry-run 已实现
6. UDP dry-run sender 已实现
7. Orin command dry-run receiver 已实现
8. Orin command receiver start/stop/status 已接入
9. 单元测试、编译检查、bash 语法检查、本机 UDP dry-run 已通过
10. 独立 xhigh 子代理安全审查通过
11. x86 -> Orin command dry-run 实机通过
12. Phase 1 OBS_COUNT=600 observation 回归通过
13. lerobot-record dry-run 短 episode 通过
14. dataset 本地落盘通过
15. Orin / x86 相关进程收尾无残留
```

当前可以进入：

```text
Phase 3 设计讨论：Orin command -> ROS command publisher / armed execution safety gate
```

当前可以宣称：

```text
Phase 2 command dry-run 链路实机验收完成
```

当前仍然不能宣称：

```text
1. 机器人真实控制已经实现
2. Orin 已经 publish ROS command topic
3. 机械臂 / 夹爪 / 底盘已经可执行 policy action
4. 当前 dry-run dataset 是最终可训练示教数据
```

Phase 2 数据边界：

```text
本阶段 record 出来的 dataset 是流程和格式验证工件。
由于 command 没有真实执行，下一帧 observation 不代表 action 后果。
该 dataset 不应作为最终 imitation learning 训练数据。
```
