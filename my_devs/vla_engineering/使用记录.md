## 单线程
```bash
python -u -m vlash_iner.run_pi05_sync \
    --policy-path /data/cqy_workspace/flexible_lerobot/outputs/pi05_eraser_cup_multi_task_runs/20260602_200955/checkpoints/last/pretrained_model \
    --robot-port /dev/serial/by-id/usb-1a86_USB_Single_Serial_5A7C123192-if00 \
    --top-cam /dev/video4 \
    --wrist-cam /dev/video6 \
    --task "Put the eraser into the small box" \
    --run-time-s 120 \
    --log-interval 1 \
    --print-action true
```

## 多线程推理
推荐主力:
  control-fps 45 / overlap 8 / blend 2

  稍快一点:
  control-fps 45 / overlap 10 / blend 2

最接近 VLASH 示例比例，也就是你当前比较成功的版本：

### 主推
  python -u -m vlash_iner.run_pi05_async \
    --policy-path /data/cqy_workspace/flexible_lerobot/outputs/pi05_eraser_cup_multi_task_runs/20260602_200955/checkpoints/last/pretrained_model \
    --robot-port /dev/serial/by-id/usb-1a86_USB_Single_Serial_5A7C123192-if00 \
    --top-cam /dev/video4 \
    --wrist-cam /dev/video6 \
    --task "Put the eraser into the small box" \
    --run-time-s 120 \
    --fps 30 \
    --control-fps 45 \
    --reuse-observation-within-chunk true \
    --inference-overlap-steps 8 \
    --background-inference true \
    --future-state-aware true \
    --chunk-blend-steps 2 \
    --log-interval 10 \
    --action-quant-ratio 1

  更贴近 VLASH 原始 30fps + overlap ratio 12.5% 的版本：

### 最接近vlash原版，但是用不了
  python -u -m vlash_iner.run_pi05_async \
    --policy-path /data/cqy_workspace/flexible_lerobot/outputs/pi05_eraser_cup_multi_task_runs/20260602_200955/checkpoints/last/pretrained_model \
    --robot-port /dev/serial/by-id/usb-1a86_USB_Single_Serial_5A7C123192-if00 \
    --top-cam /dev/video4 \
    --wrist-cam /dev/video6 \
    --task "Put the eraser into the small box" \
    --run-time-s 120 \
    --fps 30 \
    --control-fps 30 \
    --reuse-observation-within-chunk true \
    --inference-overlap-steps 6 \
    --background-inference true \
    --future-state-aware true \
    --chunk-blend-steps 2 \
    --log-interval 10 \
    --action-quant-ratio 1

  
### 快一点但是没有成功率  
    稍微激进一点但还不至于太旧 observation 的版本：

  python -u -m vlash_iner.run_pi05_async \
    --policy-path /data/cqy_workspace/flexible_lerobot/outputs/pi05_eraser_cup_multi_task_runs/20260602_200955/checkpoints/last/pretrained_model \
    --robot-port /dev/serial/by-id/usb-1a86_USB_Single_Serial_5A7C123192-if00 \
    --top-cam /dev/video4 \
    --wrist-cam /dev/video6 \
    --task "Put the eraser into the small box" \
    --run-time-s 120 \
    --fps 30 \
    --control-fps 45 \
    --reuse-observation-within-chunk true \
    --inference-overlap-steps 10 \
    --background-inference true \
    --future-state-aware true \
    --chunk-blend-steps 2 \
    --log-interval 10 \
    --action-quant-ratio 1

## torch.compile
```bash
python -u -m vlash_iner.run_pi05_compile_warmup \
    --policy-path /data/cqy_workspace/flexible_lerobot/outputs/pi05_eraser_cup_multi_task_runs/20260602_200955/checkpoints/last/pretrained_model \
    --task "Put the eraser into the small box" \
    --warmup-steps 3 \
    --compile-mode reduce-overhead \
    --run-robot true \
    --robot-port /dev/serial/by-id/usb-1a86_USB_Single_Serial_5A7C123192-if00 \
    --top-cam /dev/video4 \
    --wrist-cam /dev/video6 \
    --run-time-s 120 \
    --fps 30 \
    --control-fps 45 \
    --reuse-observation-within-chunk true \
    --inference-overlap-steps 8 \
    --background-inference true \
    --future-state-aware true \
    --chunk-blend-steps 2 \
    --log-interval 10 \
    --action-quant-ratio 1 \
    --connect-retries 3 \
    --connect-retry-s 1.0
```