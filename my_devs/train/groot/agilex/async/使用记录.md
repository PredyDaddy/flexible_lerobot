  bash my_devs/train/groot/agilex/async/run_groot_remote_async_policy_server.sh \
    --host 0.0.0.0 \
    --port 5560 \
    --policy-path /abs/path/to/your_groot_checkpoint \
    --task "Grab the brown box and put it inside the orange one." \
    --robot-type agilex \
    --backend pytorch \
    --control-arm auto \
    --socket-timeout-s 30 \
    --log-level INFO

  机器人端

  cd /data/cqy_workspace/flexible_lerobot

  bash my_devs/train/groot/agilex/async/run_groot_remote_async_robot_client.sh \
    --server-host 10.1.26.37 \
    --server-port 5560 \
    --task "Grab the brown box and put it inside the orange one." \
    --robot-type agilex \
    --backend pytorch \
    --control-mode command_master \
    --control-arm auto \
    --fps 30 \
    --low-watermark 1 \
    --chunk-size 2 \
    --response-timeout-s 0.75 \
    --reset-on-connect true \
    --dry-consume true \
    --run-time-s 60 \
    --log-level INFO