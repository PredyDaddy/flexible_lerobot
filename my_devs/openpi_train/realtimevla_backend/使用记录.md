flash_RT openpi_trt 失败

realtimevla 

启动服务
cd /data/cqy_workspace/flexible_lerobot
my_devs/openpi_train/realtimevla_backend/scripts/serve_so101_openpi.sh

cd /data/cqy_workspace/flexible_lerobot
REALTIMEVLA_INFER_URL=http://127.0.0.1:18080 \
    my_devs/openpi_train/realtimevla_backend/scripts/run_so101_client.sh \
    --execute-actions true \
    --action-chunk-steps 50 \
    --timeout-s 60

异步拉高控制频率
REALTIMEVLA_INFER_URL=http://127.0.0.1:18080 \
    my_devs/openpi_train/realtimevla_backend/scripts/run_so101_client.sh \
    --execute-actions true \
    --action-chunk-steps 30 \
    --control-fps 50 \
    --timeout-s 60 \
    --async-prefetch true \
    --prefetch-after-steps 35