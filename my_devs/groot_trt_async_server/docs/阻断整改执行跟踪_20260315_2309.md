# GR00T TRT Async Server 阻断整改执行跟踪

日期: 2026-03-15 23:09
角色: 架构协调跟踪
对应任务单: `独立复核阻断整改任务单_20260315.md`

## 当前结论

项目当前仍处于“阻断整改中”，尚未进入再次准入复核。

## T1 控制面失败终态闭环

状态: 有实质代码进展，但主线验证未收口

已确认进展:

- `robot_client.py` 中的 pure-mock 已经从原先的 helper/局部状态机检查，扩展到 entrypoint 级覆盖。
- 新增了真实入口层面的负路径和终态检查，包括：
  - `start()` 驱动的 pure-mock 入口
  - 服务端异常后的 retry terminal
  - foreign-session chunk 丢弃后的 abort terminal
  - 终态后 inflight 重新打开
  - 恢复成功后的 action chunk 回填

主线验证结果:

- 命令:
  - `conda run -n lerobot_flex python -m py_compile my_devs/groot_trt_async_server/policy_server.py my_devs/groot_trt_async_server/robot_client.py my_devs/groot_trt_async_server/run_client.py my_devs/groot_trt_async_server/run_mock_roundtrip.py`
  - `conda run -n lerobot_flex python my_devs/groot_trt_async_server/run_mock_roundtrip.py --pure-mock`
- 现状:
  - `py_compile` 通过
  - `--pure-mock` 未通过

当前失败点:

- 失败已经不在旧的协议 pack/unpack 层。
- 当前失败位于新增的 entrypoint 级负路径用例：
  - `stale observation payload mismatch`
- 实际抛错为 session mismatch：
  - `Observation payload 'session_id'='session-mismatch' does not match sticky session_id='entrypoint-session'`
- 但测试期望字符串仍是 `request_id=91`

架构判断:

- 这说明 T1 的协议和入口级覆盖正在真实落地。
- 当前问题是新增负路径用例的期望与实际错误优先级不一致，属于收口阶段问题，不再是“没有实现失败终态闭环”的原始状态。
- T1 不能算完成，必须把 `--pure-mock` 拉回通过后才可进入再次独立复核。

## T2 后端行为自证强度

状态: 仍未收口，尚未看到能改变主线输出的有效实现

最近一次主线验证:

- 命令:
  - `conda run -n lerobot_flex python -m py_compile my_devs/groot_trt_async_server/groot_trt_policy.py my_devs/groot_trt_async_server/check_groot_trt_backend.py`
  - `conda run -n lerobot_flex python my_devs/groot_trt_async_server/check_groot_trt_backend.py`
- 结果:
  - 命令执行通过
  - 但输出仍保持旧行为

未变化的阻断信号:

- timeline 仍只有：
  - `lock_enter -> stream_synchronize -> lock_exit`
- 仍然没有 session 级事件进入 timeline
- CLI summary 仍然是：
  - `all executable behavior checks and static tripwires passed; pending items still require integration`

架构判断:

- 这条线目前还没有证明“最后一次 session 工作 -> stream sync -> unlock”的顺序。
- 也还没有证明 sync 的 stream 与 session 工作 stream 存在绑定关系。
- 因此 T2 仍保持独立复核给出的阻断结论，不可准入。

## 环境阻断

状态: 未变化

- `lerobot_flex` 仍缺少 `grpc`
- 因此真实 gRPC roundtrip 仍不能在当前环境中完成

## 下一步

1. 继续收口 T1 的 pure-mock entrypoint 级负路径期望，使 `--pure-mock` 恢复全绿。
2. 对 T2 重新派发后端实现任务，要求必须改变 timeline 证据结构，而不是只维持旧输出。
3. 只有当 T1/T2 主线验证都通过后，才进入下一轮独立上下文复核。
